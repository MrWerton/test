import 'package:flutter/material.dart';
import 'package:flutter_modular/flutter_modular.dart';

class Apprax extends StatelessWidget {
  const Apprax({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Appraxi',
      home: Container(),
    ).modular();
  }
}
